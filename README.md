# sky-html-to-ios

**English** | [简体中文](README.zh-CN.md)

Convert runnable, high-fidelity HTML/CSS/JavaScript mobile prototypes into compilable and maintainable native iOS interfaces. Screenshot comparison and visual correction are optional post-generation safeguards, not conversion prerequisites.

`sky-html-to-ios` is a Codex Skill that reads computed browser layout, DOM structure, CSS, assets, and JavaScript behavior, builds an intermediate UI representation (UI IR), and generates native SwiftUI or UIKit code. The core pipeline does not require a multimodal model. It does not disguise a web page as a native app with a full-page screenshot or `WKWebView`.

## Contents

- [Why This Skill Exists](#why-this-skill-exists)
- [Design Goals](#design-goals)
- [Inputs and Outputs](#inputs-and-outputs)
- [Core Capabilities](#core-capabilities)
- [Six-Layer Native Architecture](#six-layer-native-architecture)
- [Supported Views and Controls](#supported-views-and-controls)
- [Navigation and Presentation](#navigation-and-presentation)
- [Layout and Device Adaptation](#layout-and-device-adaptation)
- [Styling, Typography, and Assets](#styling-typography-and-assets)
- [Interactions and Animation](#interactions-and-animation)
- [Project Integration](#project-integration)
- [Visual Validation](#visual-validation)
- [Requirements](#requirements)
- [Installation and Usage](#installation-and-usage)
- [HTML Authoring Guidance](#html-authoring-guidance)
- [Generated Artifacts](#generated-artifacts)
- [Scope and Limitations](#scope-and-limitations)
- [Quality Status](#quality-status)
- [FAQ](#faq)

## Why This Skill Exists

A high-fidelity HTML prototype may already describe layout, styling, component states, interactions, and multi-page flows, but it is not native iOS code. Manual conversion often introduces:

- mismatched CSS pixel and iOS point dimensions;
- reordered icons, labels, badges, counters, or trailing metadata;
- duplicated Safe Area, navigation bar, Tab Bar, or bottom-bar insets;
- accidental horizontal scrolling on a vertical page;
- native inputs and controls degraded into passive views;
- excessive fixed frames used only to match one screenshot;
- validation limited to the first viewport.

The Skill turns conversion into an evidence-based, traceable pipeline:

```text
HTML / CSS / JavaScript
          |
          v
Browser rendering and interaction discovery
          |
          v
Render Tree / Route Graph / Interaction Graph
          |
          v
UI IR
          |
          v
Native architecture, controls, assets, and naming
          |
          v
Layout relation graph and structural fidelity gate
          |
          v
SwiftUI or UIKit + Swift
          |
          v
Xcode build (core verification complete)
          |
          v optional: visual mode
Simulator captures, deterministic diff, and correction
```

## Design Goals

1. **Genuinely native output**: generate native views, controls, inputs, navigation, and page containers instead of embedding the original web page.
2. **High visual fidelity**: use computed styles, rendered rectangles, text line boxes, resolved assets, and observed states rather than guessing from source markup.
3. **Maintainable iOS architecture**: organize screens, controllers, reusable views, cells, state, navigation, and assets by responsibility.
4. **Minimal manual work**: orchestrate discovery, extraction, generation, integration, and validation; ask only when essential decisions are ambiguous.
5. **Deterministic structural fidelity**: verify source coverage, containment, visual order, spacing relations, scroll ownership, native leaves, and screen regions without screenshots or a multimodal model.
6. **Optional visual safeguards**: when visual mode is requested, connect stable HTML node IDs to UI IR and native views so a mismatch can be traced and corrected locally without making image understanding a core dependency.

## Inputs and Outputs

### Inputs

- Locally runnable HTML files
- HTML, CSS, and JavaScript high-fidelity prototypes
- Single-page and multi-page prototypes
- Responsive mobile pages
- Fixed phone artboards inside larger presentation pages
- Previously extracted and validated UI IR
- Existing Xcode projects or empty directories

Local CSS, JavaScript, images, SVG, and fonts are supported. Remote assets depend on access, licensing, and network availability.

### Output stacks

| Output | Status | Notes |
|---|---|---|
| SwiftUI + Swift | Supported | Screens, components, state, navigation, and assets |
| UIKit + Swift | Supported | UIKit controls, view controllers, and Auto Layout |
| Mixed SwiftUI/UIKit | Conditional | Selected from the target module and existing architecture |
| Objective-C | Not generated by default | Requires an explicit compatibility strategy |

The default deployment target is iOS 16 and can be configured. Newer APIs require availability checks and fallbacks.

## Core Capabilities

### Mobile page discovery

- Detects explicit mobile artboards and responsive mobile roots.
- Separates desktop presentation chrome from embedded mobile interfaces.
- Probes multiple widths when no explicit artboard exists.
- Stops when the mobile root cannot be established confidently.
- Classifies fixed, sticky, scrolling, and hide-on-scroll regions.

### Routes and interaction states

- Scans page entries, page containers, and visible states.
- Builds `html-route-graph.json` and `interaction-state-graph.json`.
- Deduplicates repeated artboards that represent one screen with a menu, sheet, alert, overlay, expanded region, or revealed swipe actions.
- Builds a separate state IR and computes generic insert, remove, replace, style/content, and geometry deltas without relying on a list of named examples.
- Associates each state artboard with one native owner screen and merges its incremental native subtree instead of generating another business screen or view controller.
- Executes deltas through native conditional subtrees, replacements, presentations, or contextual item actions; action buttons may be nested at any depth inside the state subtree.
- Protects interaction sources and ancestors from accidental removal, suppresses ambiguous deletions, and emits `state-delta-review.json` when evidence needs human review.
- Separates navigation, local state, overlays, modal presentation, and external links.
- Preserves prerequisite action sequences.
- Reports close owner matches and low-confidence transitions instead of inventing behavior; `data-ios-state-owner` can make ownership deterministic.

### System-first native controls

- Combines HTML semantics, ARIA, form attributes, JavaScript interactions, computed CSS, project components, and SDK availability.
- Prefers Apple system controls and official configuration over custom controls.
- Keeps the system control as the interaction and accessibility owner when a native wrapper is needed for shadows, gradients, clipping, or asymmetric decoration.
- Creates a custom `UIControl` only when the UI IR records evidence that system behavior or visual capability is insufficient.
- Audits the installed iPhoneOS SDK and verifies recognition, architecture planning, SwiftUI execution, UIKit execution, and compile-time availability for the supported control catalog.
- Covers every public `UIControl` subclass in the audited SDK set, plus common page controls such as `UITextView`, `UISearchBar`, `UIPickerView`, `UIProgressView`, `UIActivityIndicatorView`, and `UICalendarView`.
- Uses narrow `UIViewRepresentable` adapters only when SwiftUI has no equivalent system control; it never wraps the page in UIKit or `WKWebView`.
- Builds and validates one screenshot-free native control configuration plan for content insets, item spacing, intrinsic sizing, CSS accent/appearance evidence, track/fill/thumb/selection colors, preferred styles, and normal/highlighted/focused/editing/checked/selected/disabled/loading states; both generators consume the same matrix.

### Xcode project discovery

- Detects `.xcodeproj`, `.xcworkspace`, and `Package.swift`.
- Determines the target module's SwiftUI or UIKit architecture.
- Discovers routers, coordinators, navigation, design systems, cells, views, and assets.
- Reads deployment targets.
- Selects automatically only when the project and target are unambiguous.
- Can create a SwiftUI or UIKit app when no project exists.
- Does not silently create a host app for a standalone Swift package.

### UI IR

The intermediate representation stores:

- screens and target viewports;
- node trees with stable IDs;
- source selectors and browser rectangles;
- document flow, Flexbox, Grid, absolute, fixed, and sticky evidence;
- computed styles and text metrics;
- scroll axes and scroll/client dimensions;
- recommended native controls with mapping rationale;
- routes, state, presentation, and animation keyframes;
- asset references and SDK availability.

UI IR must pass `validate_ui_ir.py` before generation. Unresolved critical interactions block production generation.

## Six-Layer Native Architecture

Code generation does not translate the DOM directly into a flat view tree. `native-architecture-plan-1.1` makes six decisions in order:

1. **Application Container**: existing Router/Coordinator, `NavigationStack`, `UINavigationController`, `TabView`, or `UITabBarController`.
2. **Screen Container**: SwiftUI `View`, `UIViewController`, or a justified custom/child controller.
3. **Screen Regions**: system/custom top bar, content, bottom bar, floating region, overlay, and presentation ownership.
4. **Content Container**: static View/Stack, `ScrollView`/`UIScrollView`, `List`/`UITableView`, lazy Grid/`UICollectionView`, or compositional collection layout.
5. **Reusable Section and Item**: sections, headers, footers, item templates, Cell strategy, order, and scroll-axis ownership.
6. **Leaf Component**: the final `UIView`, `UIImageView`, `UILabel`, `UIButton`, input, status control, Shape, Layer, or project component with mapping evidence.

Small fixed groups remain static layouts. Long homogeneous rows use Table/Lazy containers; grids, carousels, data tables, and heterogeneous sections use Collection layouts. A collection can replace the root scroll only when it is a direct, dominant, same-axis content owner. A nested horizontal Collection keeps only the horizontal axis and never disables the page's vertical scroll. Table and Collection containers are not wrapped in another same-axis root scroll view.

Reusable containers receive an executable Section contract rather than a blanket `automaticSize`: ordered item IDs, fixed/fractional/full-width sizing, estimated content height, aspect ratio, column count, content insets, main/cross-axis spacing, directional locking, and supplementary header/footer ownership. Sticky source headers become pinned SwiftUI Section views or UIKit supplementary views; unresolved sticky ownership blocks generation.

Each screen also receives a typed layout contract containing visual child order, axis, alignment, distribution, wrapping, spacing, sizing policy, aspect ratio, and compression evidence. Inputs, explicit/project components, specialized media, and stable business controls become typed leaf views. Ordinary text, SVG internals, decorative nodes, and auto-numbered DOM leaves stay in the shared runtime instead of creating one Swift file per node.

Before Swift generation, `layout-relation-graph-1.0` promotes those per-node measurements into cross-node constraints. `structural-fidelity-report-1.0` checks that the six-layer architecture can express them, then `native-layout-plan-1.1` lowers the result into the single executable contract shared by SwiftUI and UIKit: stack/wrapping/grid/overlay algorithms, visual child order, authored CSS length expressions, computed geometry, box-model constraints, Grid tracks and item placement, positioning ownership, state-layout deltas, and compound-control slots. Parent-relative `%` and resolvable `calc()` expressions execute as parent-affine native constraints. After generation, `native-structure-manifest-1.0` proves that Swift/Payload and the native runtime consumed the required capabilities before Xcode target integration. These core gates require neither screenshots nor multimodal capability.

## Supported Views and Controls

Support levels:

- **Direct**: implemented by the core SwiftUI and UIKit generators.
- **Mapped/composed**: implemented using a system control, composed view, or custom native view according to evidence.
- **Integration**: planned natively but requires project logic, permissions, or system-framework wiring.

### Containers and layout

| HTML or role | SwiftUI | UIKit | Support |
|---|---|---|---|
| Generic container | `VStack`, `HStack`, `ZStack` | `UIView`, `UIStackView` | Direct |
| Flex row/column | Stack, spacing, alignment | `UIStackView`, priorities | Direct |
| CSS Grid | `Grid`, `LazyVGrid` | `UICollectionView`/composed layout | Direct/mapped |
| Overlay/absolute | `ZStack` | Overlay container + constraints | Direct |
| Scroll container | `ScrollView` | `UIScrollView` | Direct |
| Horizontal carousel | Horizontal `ScrollView` | `UIScrollView`/`UICollectionView` | Direct/mapped |
| Divider/spacer | `Divider`, `Spacer` | `UIView`, constraint spacer | Direct |
| Long/grouped list | `List`, sections, lazy stacks | `UITableView`/`UICollectionView` | Mapped |
| Multi-column table | Grid/custom layout | Compositional layout | Mapped |

Responsive collection lowering reads structured browser measurements at 320, 375, 393, and 430pt. It preserves `repeat()`, `minmax()`, `auto-fit`, `auto-fill`, fractional and mixed Grid tracks, then emits native adaptive columns, measured breakpoints, per-item sizing/span contracts, and text line-count evidence. SwiftUI and UIKit resolve those contracts from the actual container width instead of freezing the 393pt layout. UIKit tables use explicit plain-style geometry and native swipe/selection delegates when the prototype exposes those interactions.

Scroll-axis ownership is explicit: a nested horizontal carousel must not make the root vertical page scroll horizontally.

### Text, images, and media

| Role | SwiftUI | UIKit | Support |
|---|---|---|---|
| Single/multiline text | `Text` | `UILabel` | Direct |
| Rich text | `Text`/`AttributedString` | `NSAttributedString` | Direct |
| Selectable read-only text | Selectable `Text` | Non-editable `UITextView` | Direct |
| Image | `Image` | `UIImageView` | Direct |
| SVG/icon | Asset, Shape, SF Symbol | Asset, `CAShapeLayer`, SF Symbol | Direct/converted |
| Video | `VideoPlayer` | `AVPlayerViewController` | Integration |
| Audio | Project player | AVFoundation | Integration |
| Map | `Map` | `MKMapView` | Integration |
| PDF/file preview | Quick Look | `QLPreviewController` | Integration |

### Buttons and links

Supports text buttons, icon buttons, icon-label buttons, `role="button"`, internal routes, external URLs, menus, Tab items, target-action, SwiftUI actions, accessibility identifiers, and pressed/focused/disabled/selected states.

Composite controls are not flattened into strings. Browser geometry preserves the order and placement of icons, labels, counters, badges, and trailing metadata.

### Inputs

| HTML or role | SwiftUI | UIKit | Support |
|---|---|---|---|
| text/email/url/tel | `TextField` | `UITextField` | Direct |
| password | `SecureField` | Secure `UITextField` | Direct |
| search | `TextField`/`.searchable` | `UISearchTextField`/`UISearchController` | Direct/mapped |
| number | Numeric `TextField` | Numeric `UITextField` | Direct |
| textarea | `TextEditor` | `UITextView` | Direct |
| read-only textarea | Read-only editor wrapper | `UITextView(isEditable: false)` | Direct |
| date/time/datetime | `DatePicker` | `UIDatePicker` | Direct |
| file input | Importer entry | Document picker entry | Integration |

Input mapping covers placeholder style, value, read-only/disabled state, secure entry, line mode, maximum length, keyboard type, text content type, return key, autofocus, capitalization, autocorrection, content insets, and keyboard avoidance.

Passive text uses `Text`/`UILabel`. A read-only `TextEditor`/`UITextView` is selected only when selection, scrolling, or text-view behavior is required.

### Selection, values, and status

| HTML or role | SwiftUI | UIKit | Support |
|---|---|---|---|
| Switch/toggle | `Toggle` | `UISwitch` | Direct |
| Checkbox/radio | Custom Toggle/option | `UIButton`/`UIControl` | Direct |
| Segmented control | Segmented `Picker` | `UISegmentedControl` | Direct |
| Select/picker | `Picker`/`Menu` | `UIMenu`/picker | Direct |
| Multi-select | Multi-select menu/list | Menu/list | Direct/composed |
| Slider | `Slider` | `UISlider` | Direct |
| Stepper | `Stepper` | `UIStepper` | Direct |
| Color picker | `ColorPicker` | `UIColorWell` | Direct |
| Progress/meter | `ProgressView` | `UIProgressView` | Direct |
| Loading | `ProgressView` | `UIActivityIndicatorView` | Mapped |
| Disclosure | `DisclosureGroup`/custom | Custom `UIControl` | Mapped |
| Page indicator | Page Tab/custom | `UIPageControl` | Mapped |
| Pull to refresh | `.refreshable` | `UIRefreshControl` | Integration |
| Search bar | Local system-control adapter | `UISearchBar` | Direct/local bridge |
| Wheel picker | Wheel `Picker` | `UIPickerView` | Direct |
| Paste control | `PasteButton` | `UIPasteControl` | Direct |
| Calendar view | Local system-control adapter | `UICalendarView` | Direct/local bridge |

### System controllers and capabilities

The Skill can plan `UIActivityViewController`, document and photo pickers, camera capture, color/font pickers, printing, Quick Look, `SFSafariViewController`, contacts, mail, messages, and calendar events.

Privacy permissions, entitlements, device capabilities, framework availability, and business behavior are not inferred from presentation HTML.

## Navigation and Presentation

Supported actions include push, pop, pop-to-root, back, flow replacement, external links, Tab selection, and Tab-reselection behavior.

Supported presentations include sheets, half-height `UISheetPresentationController`, full-screen covers, modals, popovers, overlays, alerts, confirmation/action sheets, and dismiss.

| Container | SwiftUI | UIKit |
|---|---|---|
| Hierarchical navigation | `NavigationStack` | `UINavigationController` |
| Main tabs | `TabView` | `UITabBarController` |
| Master-detail | `NavigationSplitView` | `UISplitViewController` |
| Paging | Page-style `TabView` | `UIPageViewController` |
| Custom composite screen | Composed views | Custom container controller |
| Child screen | Composed structure | Complete `addChild` lifecycle |

Controllers are selected from page relationship, lifecycle, and layout evidence, not simply because the source contains a `div`.

## Layout and Device Adaptation

### Responsive pages

Responsive pages are extracted at the target viewport and normally use:

```text
1 CSS px = 1 iOS pt
```

The result uses SwiftUI Layout or Auto Layout, not whole-page runtime scaling.

### Fixed phone artboards

Fixed artboards are normalized once:

```text
designScale = targetWidthPt / sourceAppRootWidthCssPx
```

Normalized spacing, fonts, radii, and asset sizes become design tokens. Constraints still adapt the native page to other devices.

### Layout evidence

The IR represents edges, centers, intrinsic/fixed/min/max dimensions, aspect ratios, Flex grow/shrink/basis, gaps, `space-between`, auto margins, Grid columns, reverse order, absolute/fixed/sticky positioning, overflow, viewport-fixed bars, content-driven height, and multiline text height.

### Safe Area

- Each screen has one Safe Area owner.
- Scroll views use their parent's complete bounds.
- Safe Area dimensions are never subtracted twice.
- SwiftUI Safe Area or UIKit `adjustedContentInset` is used when system-managed.
- Custom navigation and bottom bars add their own height once.
- HTML-simulated status bars and Home Indicator space are not duplicated.

### Navigation bars, Tab Bars, and bottom bars

- Distinguishes native navigation from custom HTML navigation.
- Separates fixed bottom bars from scrolling footers.
- Sizes viewport bars from the parent width.
- Prevents child button widths from expanding the page.
- Preserves `flex-grow` distribution and measured icon/badge/label sizes.
- Uses scroll probes to identify sticky, collapsing, and hide-on-scroll behavior.

### Multi-size validation

Default analysis widths are 320, 375, 393, and 430 points. Native validation uses real Simulator captures and verifies that original pixels match the declared viewport at 1x, 2x, or 3x. Resizing one device screenshot to impersonate another fails the quality gate.

## Styling, Typography, and Assets

### Styling

Handles backgrounds, gradients, corner radii, borders, shadows, opacity, transforms, clipping, padding, margins, gaps, z-order, content modes, pseudo-elements, masks, filters, blur strategies, and control states.

Browser paint order is preserved as a deterministic contract: source order, pseudo-element phase, stacking-context ownership, paint group, and z-level are lowered into the same native paint rank for SwiftUI and UIKit. Corner radius remains separate from descendant clipping, so rounded containers with `overflow: visible` can still render shadows and out-of-bounds decorations.

When CSS has no direct system equivalent, the preference order is:

1. existing project component;
2. composition of system controls;
3. custom SwiftUI view;
4. custom `UIView`/`UIControl`;
5. `CALayer` or Core Graphics;
6. legal local asset fallback;
7. explicit unsupported result.

### Typography

- Records candidate and resolved fonts with fallback status.
- Supports system, serif, monospace, rounded designs, and weights 100-900.
- Preserves size, style, color, and letter spacing.
- Calibrates CSS line height with native font metrics.
- Records first and last baselines.
- Validates CJK, Latin, numbers, Emoji, and mixed runs.
- Preserves confirmed line counts and wrapping points.
- Uses expandable minimum heights for multiline text.
- Locates vertical drift by region instead of hiding it with a global offset.

### Assets

Handles images, inline/external SVG, CSS backgrounds, local fonts, Asset Catalogs, SF Symbol candidates, cover/contain, object position, repeat behavior, placeholders, and conversion manifests.

SF Symbols replace source icons only when visual appearance and semantics match. Assets with unclear licenses are not embedded automatically.

## Interactions and Animation

Interaction discovery covers click/tap, change/input, submit, expand/collapse, selection, delete/hide, navigation, presentation/dismissal, timed transitions, prerequisite sequences, and content variants. Low-confidence findings require confirmation.

Animation mapping covers CSS transitions, opacity, transforms, colors, borders, scale, rotation, translation, keyframes, springs, loading loops, navigation transitions, presentations, and gesture-driven fallback planning.

Keyframe lowering preserves sample offsets and signed translation, scale, rotation, and opacity values. Initially transparent animated nodes remain in the native hierarchy. When SVG internals or rich-text descendants are merged into one native owner, the post-generation manifest records auditable source-to-owner evidence; a captured idle SVG state is reported as a visual-only degradation rather than a completed interaction.

Validation may sample keyframes. A frame becomes a required gate only when deterministic native sampling is available. Web animation dependencies are not added automatically.

## Project Integration

### New project

When no Xcode project exists, the workflow requires a SwiftUI or UIKit choice, creates a native app, uses `Sky` as the default type prefix, connects the root screen, and validates according to the selected mode.

### Existing project

The workflow reuses project naming and components, respects routers and design systems, associates sources with an explicit target, and does not overwrite app lifecycle, authentication, deep links, Tabs, or routing. Large existing projects are not launched without confirmation by default.

### Generated structure

```text
<Target Source Root>/
└── Generated/
    └── HTMLToIOS/
        ├── Application/
        ├── Core/
        │   ├── Data/
        │   ├── Models/
        │   ├── Navigation/
        │   └── Runtime/
        ├── Home/
        │   ├── Models/
        │   ├── Screens/ or Controllers/
        │   ├── Sections/
        │   ├── Cells/
        │   └── Views/
        ├── List/
        │   ├── Models/
        │   ├── Screens/ or Controllers/
        │   ├── Sections/
        │   ├── Cells/
        │   └── Views/
        └── Resources/
            ├── Assets/
            └── Payload/
```

Features own their screens, typed UI contracts, sections, cells, and views. The architecture plan is materialized into real Swift types: UIKit registers generated `UITableViewCell`/`UICollectionViewCell` subclasses, while SwiftUI routes reusable nodes to typed item views. Shared navigation, Tab, and cross-screen runtime mechanics live under `Core`.

### Incremental updates

`.html-to-ios-generation.json` records ownership. Unmodified generated files can be updated, while manually modified files are never silently overwritten. Conflicting candidates are written separately and source-to-native traceability is preserved.

## Visual Validation

Visual validation is an optional post-generation acceptance path. The primary capability is HTML/CSS/JavaScript analysis, UI IR construction, native architecture selection, code generation, integration, and build verification. Agents without multimodal capability can use the complete core conversion pipeline. When `visual` is explicitly selected, deterministic screenshot and pixel checks still run without multimodal understanding; multimodal review only explains failures.

The validation pipeline:

1. captures required HTML states;
2. generates an isolated XCUITest target;
3. executes native actions and verifies destinations;
4. exports Simulator screenshots and node geometry;
5. normalizes captures to logical viewports;
6. compares pixels, regions, text edges, and geometry;
7. generates comparisons, overlays, heatmaps, and reports;
8. performs multimodal review when supported by the active agent.

Quality checks cover image dimensions, global mismatch, mean absolute difference, critical regions, text edges, vertical drift, node geometry, capture coverage, unintended overflow, scroll-axis ownership, responsive layout, required states, and actual presentation visibility.

Repeated-state artboards are captured independently. Each state carries its own HTML root selector, active native state, geometry nodes, and validation regions, while iOS capture executes the real tap, swipe, or presentation action.

Failed visual gates produce an iteration-scoped `visual-correction-plan.json`, which attributes differences to UI IR nodes and proposes bounded corrections to layout contracts, text calibration, assets, presentation strategy, state deltas, or system-control configuration. High-confidence, state-safe, small geometry mutations can be applied to a copied UI IR and then regenerated and revalidated. Low-confidence or non-deterministic appearance changes remain review items. Original IR and generated Swift source are never overwritten by correction patches.

Geometry instrumentation expands the accessibility tree only under generated UI-test launch arguments and does not alter normal production accessibility.

| Mode | Behavior |
|---|---|
| `auto` | Builds new projects without launching the Simulator; asks before validating existing projects |
| `ask` | Generates and integrates, then waits |
| `build` | Runs an Xcode build only |
| `visual` | Builds, captures, and applies visual gates |
| `none` | Skips validation and cannot be reported as compiled or visually verified |

## Requirements

- macOS
- Xcode; iOS Simulator is required only for optional `visual` verification
- Python 3.9+
- Ruby
- Node.js
- Playwright and Chromium
- Pillow
- A locally runnable HTML prototype
- Write access to the destination project

Node resolution checks explicit `--node`, `CODEX_NODE`, `PATH`, and the bundled Codex Node runtime. Xcode validation requires macOS.

## Installation and Usage

Clone into the Skills directory used by your Codex environment:

```bash
git clone https://github.com/skyzizhu/sky-html-to-ios.git \
  ~/.codex/skills/sky-html-to-ios
```

Example Codex request:

```text
Use $sky-html-to-ios to convert mobile-prototype.html into native
SwiftUI screens.
```

Resolve the Skill directory and run from the user's project:

```bash
SKILL_ROOT="$HOME/.codex/skills/sky-html-to-ios"

python3 "$SKILL_ROOT/scripts/run_html_to_ios.py" \
  --workspace "$PWD" \
  --html /absolute/path/prototype.html \
  --ui-stack swiftui \
  --verification-mode auto
```

Use `--verification-mode visual` only when screenshot comparison and visual correction are desired.

For UIKit, use `--ui-stack uikit`. For an existing project:

```bash
python3 "$SKILL_ROOT/scripts/run_html_to_ios.py" \
  --workspace "$PWD" \
  --html /absolute/path/prototype.html \
  --project /absolute/path/App.xcodeproj \
  --target App \
  --verification-mode ask
```

Dry run:

```bash
python3 "$SKILL_ROOT/scripts/run_html_to_ios.py" \
  --workspace "$PWD" \
  --html /absolute/path/prototype.html \
  --dry-run
```

All options:

```bash
python3 "$SKILL_ROOT/scripts/run_html_to_ios.py" --help
```

## HTML Authoring Guidance

For better results:

- use semantic `button`, `input`, `textarea`, `select`, and `nav`;
- describe custom controls with `role` and `aria-*`;
- provide stable roots and IDs;
- expose recognizable page states and routes;
- include a viewport meta tag;
- make animation triggers and initial state deterministic;
- keep local assets accessible;
- avoid backend dependencies required only to render the prototype;
- use the optional `data-ios-*` contract when native intent is ambiguous.

Avoid screenshot-only input, anonymous actions, random DOM IDs, inaccessible cross-origin assets, and whole-app Canvas/WebGL rendering. See `references/html-authoring-contract.md`.

## Generated Artifacts

Reports are written under `<workspace>/.html-to-ios/`.

| File | Purpose |
|---|---|
| `orchestration-report.json` | Pipeline status and gates |
| `project-generation-decision.json` | Project, stack, and verification decisions |
| `html-route-graph.json` | Route graph |
| `interaction-state-graph.json` | Interaction state graph |
| `ios-project-report.json` | Project discovery |
| `ios-component-index.json` | Reusable component index |
| `ios-sdk-report.json` | SDK availability |
| `native-naming-plan.json` | Prefixes and conflicts |
| `native-architecture-plan.json` | Navigation, scrolling, Safe Area, and presentation |
| `layout-relation-graph.json` | Containment, visual order, spacing, alignment, aspect ratio, overlap, and scroll ownership |
| `structural-fidelity-report.json` | Screenshot-free source-to-native structural quality gate |
| `native-layout-plan.json` | Executable layout contract shared by SwiftUI and UIKit |
| `native-layout-plan-validation.json` | Pre-generation layout lowering gate |
| `native-control-configuration-plan.json` | Shared internal geometry, appearance, style, and state contract for system controls |
| `native-control-configuration-validation.json` | Screenshot-free native control configuration gate |
| `native-structure-manifest.json` | Actual Swift/Payload node and relation consumption evidence |
| `native-structure-validation.json` | Post-generation structural gate run before target integration |
| `screens/<id>/render-tree.json` | Browser render tree |
| `screens/<id>/ui-ir.json` | Screen UI IR |
| `screens/<id>/text-calibration.json` | Text calibration |
| `screens/<id>/responsive-layout.json` | Responsive analysis |
| `screens/<id>/scroll-region-behavior.json` | Scroll behavior |
| `screens/<id>/visual-state-manifest.json` | Required states |
| `screens/<id>/state-delta-review.json` | State ownership, strategy confidence, protected nodes, and suppressed deletions |
| `screens/<id>/visual-review/iteration-<n>/visual-correction-plan.json` | Node-level attribution, bounded mutations, and rejection reasons |
| `screens/<id>/visual-corrections/iteration-<n>/` | Corrected UI IR and application report |
| `screens/<id>/visual-review/iteration-<n>/` | Diffs, overlays, heatmaps, and reports |

## Scope and Limitations

The Skill does not invent API behavior, authentication, payment, databases, server validation, pagination, push logic, analytics configuration, absent pages, undefined error handling, or privacy setup.

Not supported by default:

- screenshot-only generation of a maintainable app;
- full-page `WKWebView` conversion;
- screenshot composition;
- WebGL or complex Canvas apps;
- inaccessible third-party embeds;
- DRM/player business logic;
- game-engine conversion;
- automatic Objective-C generation;
- guarantees that arbitrary HTML reaches 100% pixel identity automatically.

High-risk inputs include runtime-generated DOM, closed Shadow DOM, cross-origin iframes, advanced shaders/filters, hover-only mobile interactions, nondeterministic animation, ambiguous breakpoints, missing fonts, inaccessible assets, and ambiguous Xcode targets.

## Quality Status

The first usable release has passed:

- real SwiftUI and UIKit builds;
- 132 automated tests in the current development audit;
- UI IR, decision, naming, generator, route, and interaction tests;
- visual-difference and geometry tests;
- real Simulator responsive matrices;
- Skill structure validation;
- Python, Ruby, and Node checks.

“Usable” primarily means the workflow can analyze HTML/CSS/JavaScript, generate and integrate native code, and compile it. Optional visual mode can additionally capture, compare, and iteratively correct native interfaces. Complex HTML may still require targeted node-level review when that mode is selected.

## FAQ

### Why choose SwiftUI or UIKit for a new project?

A new project has no architecture to infer. An explicit choice prevents the entire output from using the wrong stack.

### Why not launch an existing project automatically?

Existing apps may be large or require authentication, environment variables, and backend services. The default workflow asks before spending time on a build or launch.

### Why not use fixed frames everywhere?

Fixed frames can match one screenshot but fail across devices, text changes, and Safe Areas. Browser dimensions become evidence for SwiftUI Layout or Auto Layout constraints.

### Why can visual comparison be below 100%?

Browsers and iOS differ in font rasterization, shadows, gradients, antialiasing, and control rendering. Reports expose these differences instead of relabeling failures as passes.

### What if the HTML has no phone artboard?

Stable responsive pages are probed at mobile widths. The workflow asks for clarification when the mobile root or breakpoint remains ambiguous.

### What if there is no native equivalent?

The workflow prefers existing project components, system compositions, custom views, `UIControl`, `CALayer`, or Core Graphics. If none is reasonable, the element is reported as unsupported rather than hidden behind a screenshot or WebView.

### Can generated files be updated?

Yes. The manifest protects manual changes and records conflicts. General fidelity fixes belong in Skill rules or UI IR rather than one-off controller patches.

### What defines a complete conversion?

UI IR must pass, critical interactions must be resolved, files must belong to the intended target, the selected verification mode must pass, and all fallbacks must be reported. Required screenshots and visual states are mandatory only when `visual` verification was selected.

## Repository

- GitHub: <https://github.com/skyzizhu/sky-html-to-ios>
- Skill name: `sky-html-to-ios`
